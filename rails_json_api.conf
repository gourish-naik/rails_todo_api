server {
    listen 80;
    server_name localhost;

    root /home/app/rails_json_api/public;

    passenger_enabled on;
    passenger_user app;

    # The following line can be removed if you're not using Rails' asset pipeline.
    passenger_app_env development;
}